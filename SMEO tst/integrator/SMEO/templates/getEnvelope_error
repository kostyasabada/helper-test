{
    "status": "true",
    "errorOccured": "true",
    "bpmnError": {
        "bpmnErrorCode": "SigningStatusCheckFailed",
        "bpmnErrorMessage": "<#if body.message?? && body.message?has_content>${body.message}<#else>Check signing status call failed</#if>"
    }
}