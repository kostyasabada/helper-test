<#if body.header.Status == "Ok">
    <@printObjectToJson source=body />
<#elseif body.header.Status == "Nok">
{   
    "status": "true",
    "bpmnError": {
        "bpmnErrorCode": "HandleMasterLeaseAgreementFailure",
        "bpmnErrorMessage": "${body.header.Message}"
    }
}
</#if>

