{
    "modifications": {
    <#list body.variablesToSet as name, value>
        "${name}" : {
            "value": "${value}",
            "type": "${body.types['${name}']}"
        }
        <#sep>,</#sep>
    </#list>    
    }
}